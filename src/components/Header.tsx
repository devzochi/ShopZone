import React from 'react';
import { Heart, Search, ShoppingBag, X } from 'lucide-react';
import { CategoryItem } from '../types';
interface HeaderProps { activeTab: 'shop' | 'deals' | 'new'; setActiveTab: (tab: 'shop' | 'deals' | 'new') => void; selectedCategory: string | null; setSelectedCategory: (category: string | null) => void; categories: CategoryItem[]; cartCount: number; wishlistCount: number; onOpenCart: () => void; onOpenWishlist: () => void; onOpenSearch: () => void; searchQuery: string; setSearchQuery: (query: string) => void; }
export const Header: React.FC<HeaderProps> = ({ activeTab, setActiveTab, selectedCategory, setSelectedCategory, categories, cartCount, wishlistCount, onOpenCart, onOpenWishlist, onOpenSearch, searchQuery, setSearchQuery }) => <header className="sticky top-0 z-40 bg-white/95 backdrop-blur-md border-b border-neutral-200"><div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8"><div className="flex items-center justify-between h-18 gap-3 sm:gap-6"><button onClick={() => { setActiveTab('shop'); setSelectedCategory(null); setSearchQuery(''); }} className="text-2xl font-bold tracking-tight text-[#b93815] cursor-pointer">ShopZone</button><div className="flex-1 max-w-sm hidden sm:block relative"><Search className="absolute left-3.5 top-1/2 -translate-y-1/2 w-4 h-4 text-neutral-400" /><input value={searchQuery} onChange={(event) => setSearchQuery(event.target.value)} onFocus={onOpenSearch} placeholder="Search products..." className="w-full bg-[#f4f4f5] focus:bg-white text-sm rounded-xl pl-10 pr-9 py-2 border border-transparent focus:border-neutral-300 focus:outline-none" />{searchQuery && <button onClick={() => setSearchQuery('')} className="absolute right-2.5 top-1/2 -translate-y-1/2 text-neutral-400 cursor-pointer"><X className="w-3.5 h-3.5" /></button>}</div><nav className="hidden md:flex items-center space-x-6 text-sm font-medium">{(['shop', 'deals', 'new'] as const).map((tab) => <button key={tab} onClick={() => { setActiveTab(tab); setSelectedCategory(null); }} className={`py-1.5 cursor-pointer ${activeTab === tab ? 'text-neutral-900 font-semibold border-b-2 border-[#b93815]' : 'text-neutral-600 hover:text-neutral-900'}`}>{tab === 'new' ? "What's New" : tab[0].toUpperCase() + tab.slice(1)}</button>)}</nav><div className="flex items-center space-x-2"><button onClick={onOpenSearch} className="sm:hidden p-2 text-neutral-700 cursor-pointer" aria-label="Search"><Search className="w-5 h-5" /></button><button onClick={onOpenWishlist} className="relative p-2 text-neutral-700 cursor-pointer" aria-label="Wishlist"><Heart className="w-5 h-5" />{wishlistCount > 0 && <span className="absolute -top-0.5 -right-0.5 min-w-[18px] h-[18px] bg-[#b93815] text-white text-[10px] font-bold rounded-full flex items-center justify-center px-1">{wishlistCount}</span>}</button><button onClick={onOpenCart} className="relative p-2 text-neutral-700 cursor-pointer" aria-label="Shopping cart"><ShoppingBag className="w-5 h-5" />{cartCount > 0 && <span className="absolute -top-0.5 -right-0.5 min-w-[18px] h-[18px] bg-[#b93815] text-white text-[10px] font-bold rounded-full flex items-center justify-center px-1">{cartCount}</span>}</button></div></div></div><div className="border-t border-neutral-100 bg-white"><div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8"><div className="flex items-center justify-center overflow-x-auto py-2.5 no-scrollbar gap-6 sm:gap-10 text-xs sm:text-sm font-medium text-neutral-600">{categories.map((category) => <button key={category.id} onClick={() => { setActiveTab('shop'); setSelectedCategory(selectedCategory === category.slug ? null : category.slug); }} className={`whitespace-nowrap px-2.5 py-1 rounded-md cursor-pointer ${selectedCategory === category.slug ? 'text-[#b93815] bg-[#b93815]/10 font-semibold' : 'hover:text-neutral-900 hover:bg-neutral-50'}`}>{category.name}</button>)}</div></div></div></header>;
